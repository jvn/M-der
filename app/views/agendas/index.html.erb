<h1>Listing agendas</h1>

<table>
  <tr>
    <th>Meetingnr</th>
    <th>Punktnr</th>
    <th>Punktnavn</th>
    <th>Beskrivelse</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @agendas.each do |agenda| %>
  <tr>
    <td><%= agenda.meetingnr %></td>
    <td><%= agenda.punktnr %></td>
    <td><%= agenda.punktnavn %></td>
    <td><%= agenda.beskrivelse %></td>
    <td><%= link_to 'Show', agenda %></td>
    <td><%= link_to 'Edit', edit_agenda_path(agenda) %></td>
    <td><%= link_to 'Destroy', agenda, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Agenda', new_agenda_path %>
